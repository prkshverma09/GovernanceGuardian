[pytest]
markers =
    unit: small, isolated tests with no external dependencies
    integration: tests interacting with real Elasticsearch cluster
    e2e: tests validating full end-to-end user journeys
testpaths = tests
python_files = test_*.py
addopts = -v
